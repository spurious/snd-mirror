#ifndef MUS_CONFIG_H_LOADED
#define MUS_CONFIG_H_LOADED

/* --disable-deprecated */
#undef DISABLE_DEPRECATED

/* --with-temp-dir */
#undef MUS_DEFAULT_TEMP_DIR

/* --with-save-dir */
#undef MUS_DEFAULT_SAVE_DIR

/* --with-doc-dir */
#undef MUS_DEFAULT_DOC_DIR

/* --with-editres */
#undef MUS_WITH_EDITRES

/* --with-snd-as-widget */
#undef SND_AS_WIDGET

/* --with-gmp */
#undef WITH_GMP

/* --with-gl2ps */
#undef WITH_GL2PS

/* --with-pulseaudio */
#undef MUS_PULSEAUDIO

/* --with-portaudio -- obsolete? hard to tell from the website
 */
#undef MUS_PORTAUDIO




#undef HAVE_LADSPA
/* --with-ladspa or it finds ladspa.h?
 *   just linux I think
 */

#undef HAVE_ACCESS
#undef HAVE_VASPRINTF
#undef HAVE_LSTAT
#undef HAVE_SIGNAL
/* not windows, everywhere else
 */


#undef HAVE_READLINK
#undef HAVE_GETLINE
#undef HAVE_SETLOCALE
#undef HAVE_SLEEP
#undef HAVE_OPENDIR

#undef USE_STATVFS
#undef USE_STATFS
/* these two pertain to linux/bsd -- vfs for the former? */

#undef HAVE_DLOPEN

#undef HAVE_SPECIAL_FUNCTIONS
/* libc version? */

#undef WORDS_BIGENDIAN

#ifdef __SIZEOF__POINTER__
  #define SIZEOF_VOID_P __SIZEOF_POINTER__
#else
  #undef SIZEOF_VOID_P
#endif


#undef HAVE_READLINE
/* this is only used in ruby xen_rb_repl xen.c -- make -D as --with-ruby? 
   on the explicit --enable-readline switch already (for disable case)
   then checks for libreadline, adds that and -lncurses
   linux=yes
   TODO: try ruby in openbsd, freebsd, mac, assume windows=no
 */

#undef HAVE_DLFCN_H
/* linux/bsd? (not windows I think, not osx if c++?) 
   TODO: dlfcn in freebsd?  in osx in c?
 */

#undef HAVE_SYS_SOUNDCARD_H
#undef HAVE_MACHINE_SOUNDCARD_H
/* sys in fc11 linux --where in fc19?, which in bsd? (neither in openbsd) 
 */

#undef HAVE_SYS_MIXER_H
/* solaris case -- do we care anymore? */

#undef HAVE_ALSA_ASOUNDLIB_H
/* alsa.pc? */

#undef HAVE_BYTESWAP_H
/* not osx or windows -- TODO check osx c here
 */


#undef HAVE_OSS
#undef HAVE_ALSA
#undef MUS_JACK

#undef HAVE_AUDIODEVICEDESTROYIOPROCID
/* this was old OSX -- can we use OS version? where is it? */

#undef WITH_AUDIO
/* meant for --without-audio but how to fit in?
 */

#undef HAVE_FFTW3
/* can be either fftw.pc or (locally built?) fftw3.pc
 */

#undef HAVE_GSL
/* gls.pc */

#undef HAVE_SCHEME
#undef HAVE_RUBY
#undef HAVE_FORTH
/* these can be arg choices via -D */

#undef HAVE_GL
/* gl.pc -- explicit arg
 */

#undef JUST_GL
/* maybe put the gl scm stuff under the autoloader so we don't need this switch
 *   TODO: try this everywhere and see if it is even needed anymore
 *      linux:no, windows:no, 
 *   if so, maybe remove makegl.scm gl.c gldata.scm, or make them explicitly cloaded (not ruby or forth in that case)
 */

#undef HAVE_GLU
/* glu.pc -- why this? (gl.c and snd-chn.c) -- it's for graph cursor handling I think
 */


#undef USE_GTK
/* gtk-2.0.pc etc */

#undef USE_MOTIF
#undef USE_NO_GUI
/* --without-gui or get it from the others
 */

#undef HAVE_X
/* surely !_MSC_VER! -- no there's the OSX case */

#undef RUBY_SEARCH_PATH
/* why this?
 *   used in snd-xen.c to set up .rb search paths I think, could be handled at runtime as
 *   ruby -e 'puts $:.join(":")'
 * but is there a ruby function for this? rb_gv_get("$:")

      fprintf(stderr, "search: %s\n", RUBY_SEARCH_PATH);
      {
	XEN s;
	s = rb_gv_get("$:");
	fprintf(stderr, "got: %s\n", XEN_AS_STRING(s));
      }

search: /usr/local/lib/ruby/site_ruby/2.0.0:/usr/local/lib/ruby/site_ruby/2.0.0/x86_64-linux:/usr/local/lib/ruby/site_ruby:/usr/local/lib/ruby/vendor_ruby/2.0.0:/usr/local/lib/ruby/vendor_ruby/2.0.0/x86_64-linux:/usr/local/lib/ruby/vendor_ruby:/usr/local/lib/ruby/2.0.0:/usr/local/lib/ruby/2.0.0/x86_64-linux
got: ["/home/bil/ruby-snd", "/usr/local/share/snd", "/usr/local/lib/ruby/site_ruby/2.0.0", "/usr/local/lib/ruby/site_ruby/2.0.0/x86_64-linux", "/usr/local/lib/ruby/site_ruby", "/usr/local/lib/ruby/vendor_ruby/2.0.0", "/usr/local/lib/ruby/vendor_ruby/2.0.0/x86_64-linux", "/usr/local/lib/ruby/vendor_ruby", "/usr/local/lib/ruby/2.0.0", "/usr/local/lib/ruby/2.0.0/x86_64-linux"]

see xen_rb_add_to_load_path in xen.c
 */

#undef WITH_SYSTEM_EXTRAS
/* this is an s7 switch, used also in xen.c, but it should be an autoloaded library in s7, like getenv, so this switch is almost obsolete 
   why is it here at all?
 */

#undef HAVE_OGG
#undef PATH_OGGDEC
#undef PATH_OGGENC
/* ogg.pc, exec_prefix/bin/ogg* */

#undef HAVE_FLAC
#undef PATH_FLAC
#undef HAVE_SPEEX
#undef PATH_SPEEXDEC
#undef PATH_SPEEXENC
#undef HAVE_MPEG
#undef PATH_MPG123
#undef HAVE_TIMIDITY
#undef PATH_TIMIDITY
#undef HAVE_MPG321
#undef PATH_MPG321
#undef HAVE_TTA
#undef PATH_TTA
#undef HAVE_WAVPACK
#undef PATH_WAVPACK
#undef PATH_WVUNPACK

/* ---------------------------------------- */

#ifndef USE_SND
  #define USE_SND 1
#endif

#define HAVE_EXTENSION_LANGUAGE (HAVE_SCHEME || HAVE_RUBY || HAVE_FORTH)

#define HAVE_COMPLEX_NUMBERS    ((!_MSC_VER) && ((!HAVE_FORTH) || HAVE_COMPLEX))   /* the latter defined in fth-lib.h I think */
#define HAVE_COMPLEX_TRIG       ((!_MSC_VER) && (!__cplusplus) && (!__FreeBSD__))  /* this assumes sys/param.h has been included */
#define HAVE_MAKE_RATIO ((HAVE_SCHEME) || (HAVE_FORTH))

/* TODO: forth might be 0 here and in the complex cases, fth_make_complex and fth_ratio_floor
 *   can we use a version number? ratio after 1.2.9: 1.3.6? complex always from 1.3.1?
 *   but FTH_VERSION is a function call, and FTH_PACKAGE_VERSION is a string
 */

#ifdef _MSC_VER
  #define ssize_t int 
  #define snprintf _snprintf 
  #define strtoll strtol
  #if _MSC_VER > 1200
    #define _CRT_SECURE_NO_DEPRECATE 1
    #define _CRT_NONSTDC_NO_DEPRECATE 1
    #define _CRT_SECURE_CPP_OVERLOAD_STANDARD_NAMES 1
  #endif
#endif
#endif
